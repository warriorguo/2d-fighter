{
  "name": "2d-fighter",
  "version": "0.1.0",
  "private": true,
  "description": "2D vertical scrolling airplane shooter with co-op networking",
  "license": "MIT",
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=shared && npm run build --workspace=client && npm run build --workspace=server",
    "build:shared": "npm run build --workspace=shared",
    "test": "npm run test --workspace=shared"
  }
}
